// Shared by browser and build-time configuration so starter fallbacks cannot drift.
export const DEFAULT_SITE = Object.freeze({
  name: "OpenGateway Starter",
  description: "A reusable React starter for AI gateway sites and clearly labeled product demos.",
  siteUrl: "https://starter.example.com",
  repositoryUrl: "https://github.com/YouRen1320/open-gateway-starter",
});
